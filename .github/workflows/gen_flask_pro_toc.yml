name: TOC Generator gen_flask_pro
on:
  push:
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: TOC Generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v2

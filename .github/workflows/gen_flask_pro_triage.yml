name: gen_flask_pro triage
on:
  issues:
    types: [opened]
jobs:
  commentOnNewIssues:
    name: gen_flask_pro triage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: gen_flask_pro triage
        uses: actions/github@v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: comment "[gen_flask_pro] should be supported for Python 2.7, 3.5, 3.6, 3.7, 3.8, 3.9"
